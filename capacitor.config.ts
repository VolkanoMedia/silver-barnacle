import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.volkano.es',
  appName: 'CanariasD',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
